'use client';

import React from 'react';
import Link from 'next/link';
import { Deal } from '@/lib/types';
import { formatPrice, getPlatformInfo } from '@/lib/data';

interface RelatedDealsProps {
  deals: Deal[];
}

const RelatedDeals: React.FC<RelatedDealsProps> = ({ deals }) => {
  if (deals.length === 0) return null;

  return (
    <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
      <div className="flex items-center justify-between mb-6">
        <h3 className="text-xl font-bold text-gray-800 flex items-center">
          <span className="mr-2">🔗</span>
          연관 상품
        </h3>
        <span className="text-sm text-gray-500">
          비슷한 카테고리의 다른 딜들
        </span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {deals.map((deal) => {
          const platformInfo = getPlatformInfo(deal.platform);
          
          return (
            <Link
              key={deal.id}
              href={`/deals/${deal.id}`}
              className="group bg-white rounded-lg border border-gray-200 overflow-hidden hover:shadow-lg transition-all duration-300 hover:scale-105"
            >
              {/* 이미지 영역 */}
              <div className="relative">
                <div className={`h-48 bg-gradient-to-br ${deal.imageGradient} flex items-center justify-center`}>
                  <span className="text-4xl group-hover:scale-110 transition-transform duration-300">
                    {deal.imageIcon}
                  </span>
                </div>
                
                {/* 할인율 배지 */}
                <div className="absolute top-2 right-2">
                  <span className="bg-red-600 text-white px-2 py-1 rounded text-sm font-bold">
                    -{deal.discountRate}%
                  </span>
                </div>

                {/* 태그 */}
                {deal.tags.slice(0, 1).map((tag, index) => (
                  <div key={index} className="absolute top-2 left-2">
                    <span className={`px-2 py-1 rounded text-xs font-bold ${
                      tag === 'HOT' ? 'bg-red-500 text-white animate-pulse' :
                      tag === 'NEW' ? 'bg-blue-500 text-white' :
                      tag === '최저가' ? 'bg-green-500 text-white' :
                      tag === '마감임박' ? 'bg-orange-500 text-white' :
                      'bg-gray-500 text-white'
                    }`}>
                      {tag === 'HOT' && '🔥'} 
                      {tag === 'NEW' && '✨'}
                      {tag === '최저가' && '💰'}
                      {tag === '마감임박' && '⏰'}
                      {tag}
                    </span>
                  </div>
                ))}
              </div>

              {/* 상품 정보 */}
              <div className="p-4">
                <h4 className="font-medium text-gray-800 text-sm leading-relaxed line-clamp-2 mb-2 group-hover:text-blue-600 transition-colors">
                  {deal.title}
                </h4>

                {/* 플랫폼 */}
                <div className="mb-2">
                  <span className={`${platformInfo.color} text-xs font-medium ${platformInfo.bgColor} px-2 py-1 rounded`}>
                    {deal.platform}
                  </span>
                </div>

                {/* 가격 */}
                <div className="flex items-center justify-between">
                  <div>
                    <div className="text-red-600 font-bold">
                      {formatPrice(deal.dealPrice)}
                    </div>
                    <div className="text-gray-400 text-sm line-through">
                      {formatPrice(deal.originalPrice)}
                    </div>
                  </div>
                  
                  <div className="text-xs text-gray-500">
                    {deal.timeAgo}
                  </div>
                </div>

                {/* 호버 시 보기 버튼 */}
                <div className="mt-3 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                  <div className="bg-blue-500 text-white text-center py-2 rounded text-sm font-medium">
                    상세보기 →
                  </div>
                </div>
              </div>
            </Link>
          );
        })}
      </div>

      {/* 더보기 버튼 */}
      <div className="mt-6 text-center">
        <Link
          href="/"
          className="inline-flex items-center space-x-2 text-blue-600 hover:text-blue-800 font-medium"
        >
          <span>더 많은 딜 보러가기</span>
          <span>→</span>
        </Link>
      </div>
    </div>
  );
};

export default RelatedDeals;